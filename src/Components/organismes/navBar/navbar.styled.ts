import styled from 'styled-components';

export const NavBarContainer = styled.nav`
    padding: 2rem;
    background-color: var(--primary-color);
`;

export const Title = styled.h1`
    color: var(--black-color);
    font: 500 1.625rem / normal Kanit;
`;
